# quick-access-terminal.conf — zshkit starter config
# Launch with: kitten quick-access-terminal --detach

lines 18
edge top
hide_on_focus_loss yes
background_opacity 0.96

# Keep the quick-access terminal visually close to the main Kitty setup.
kitty_override font_size=14.0
kitty_override window_padding_width=6
